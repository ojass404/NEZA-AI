# Keep backend model directory structure
echo "# Keep directory" > backend/models/configs/.gitkeep

# Keep upload directories
echo "# Keep directory" > backend/uploads/.gitkeep
echo "# Keep directory" > backend/reports/.gitkeep

# Keep ML data directories
echo "# Keep directory" > ml_pipeline/data/raw/.gitkeep
echo "# Keep directory" > ml_pipeline/data/processed/.gitkeep
echo "# Keep directory" > ml_pipeline/data/labels/.gitkeep

# Keep tests fixtures
echo "# Keep directory" > backend/tests/fixtures/.gitkeep

# Keep logs directory
echo "# Keep directory" > logs/.gitkeep